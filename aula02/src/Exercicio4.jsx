export default function Exercicio4 ( {tipo} {base} {altura} {area} )
{
   let areaq = Number(base) * Number ( area)  
   let areat = Number (2/( (base) * (altura)))

   return (
     <div>
        area do triangulo e {areat}
     </div>
     
   )
}